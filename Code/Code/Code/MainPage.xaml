<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:Code"
       x:Class="Code.MainPage"
       Title="SO">

    <Shell.Resources>
        <ResourceDictionary>
            <Color x:Key="HeaderColor">#5dbf63</Color>
        </ResourceDictionary>
    </Shell.Resources>

    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid BackgroundColor="{StaticResource HeaderColor}"
              HeightRequest="200">
                <Image Aspect="AspectFill"
                   Source="xamarinstore.jpg"
                   Opacity="0.6" />
            </Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    <FlyoutItem Title="Schedule" Icon="schedule_item.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Schedule}" />
    </FlyoutItem>

    <FlyoutItem Title="Events" Icon="event_icon.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Events}" />
    </FlyoutItem>

    <FlyoutItem Title="Note" Icon="notes_icon.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Notes}" />
    </FlyoutItem>

    <FlyoutItem Title="Homework" Icon="homework_icon.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Homework}" />
    </FlyoutItem>
</Shell>
